{
  programs.nvf = {
    enable = true;

    settings.vim = {
      viAlias = true;
      vimAlias = true;

      lsp = {
        enable = true;

      };

      utility = {


        motion = {
          # hop.enable = true;
          # leap.enable = true;
          precognition.enable = true;
        };
      };
    };
  };
}
